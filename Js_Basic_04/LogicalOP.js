//logical operators are used to combine multiple conditions and return a boolean result (true or false)

let b = true, c = false;

console.log(b && c); // false (logical AND)
console.log(b || c); // true (logical OR)
console.log(!b); // false (logical NOT)
console.log(!c); // true (logical NOT)