let str = "hello World!";
console.log(str.length);

//access by index
console.log(str[0]);
console.log(str[7]);
console.log(str.at(-1));

str.charAt(0); // return zero index value

str.charCodeAt(1); // unicode character returned for 1 index

